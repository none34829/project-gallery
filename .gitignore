.env
.config.js
.DS_Store
data.json
# dist is kept committed as a build-output fallback; Vercel rebuilds it from
# source on every deploy, so it is intentionally not ignored here.
# dist
node_modules
package-lock.json
src/assets/images/student_imgs/*
src/assets/images/mentor_imgs/*
src/assets/pdfs/*
!**/.keep
.vercel
_shots/
_broken_images.json
